const NextImages = require('next-images')
module.exports ={
    images: {
        formats: ['image/webp']
    },
}
/** @type {import('next').NextConfig} */
const nextConfig = {
    distDir: '.next',
  };